If Application cannot be built, firsly start Android Device Manager then start Emulator (API 12-31).
Secondly, go back to application and run it on that emulator.
If data not shown, then restart application. 
